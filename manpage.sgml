<!doctype linuxdoc system>
<manpage title="FLACTAG" sectnum="6">
<sect1>NAME
<p>

flactag - program to tag multi-track FLAC files with embedded CUE sheets using 
data from the MusicBrainz service

<sect1>SYNOPSIS
<p>
Just use it
<sect1>DESCRIPTION
<p>
<em>flactag</em> is a utility for tagging single album FLAC files with embedded CUE sheets 
using data from the MusicBrainz service.
<sect1>OPTIONS
<sect2>--check | -c
<p>
Check tags against those currently in the file. Let's see what happens if this is a really long sentence that is more than a line.

<sect2>--write | -w
Write tags to the file if they are different
<sect1>INTERACTIVE MODE
<p>

<sect1>AUTHOR
<p>

</manpage>
